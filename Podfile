# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'sleepTrackerHackathon (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for sleepTrackerHackathon (iOS)
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'

end

target 'sleepTrackerHackathon (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for sleepTrackerHackathon (macOS)
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'

end
